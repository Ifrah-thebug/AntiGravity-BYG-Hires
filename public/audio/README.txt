# Optional portfolio story music
# Drop a looping MP3 here (e.g. soft storybook / ambient track):
#   portfolio-story.mp3
#
# If missing, the page uses a built-in gentle music-box synth instead.
#
# Or set a custom URL in .env:
#   VITE_PORTFOLIO_MUSIC_URL=https://your-cdn.com/track.mp3
